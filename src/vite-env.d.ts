/// <reference types="vite/client" />

// Injected by vite.config.ts `define` — a per-build string used to bust the
// persisted React Query cache across deploys (see lib/queryPersist.ts).
declare const __BUILD_TIME__: string
